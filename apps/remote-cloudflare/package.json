{
  "name": "@kelp/remote-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "test": "cargo build --locked --package kelp-cli && node --test test/remote.test.mjs",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260918.1",
    "@types/node": "24.13.5",
    "typescript": "5.9.3",
    "wrangler": "4.135.0"
  }
}
